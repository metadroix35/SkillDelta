services:
  - type: web
    name: skilldelta-backend
    runtime: python
    buildCommand: |
      apt-get update
      apt-get install -y tesseract-ocr poppler-utils
      pip install -r requirements.txt
    startCommand: uvicorn skillrot_app.main:app --host 0.0.0.0 --port 10000
